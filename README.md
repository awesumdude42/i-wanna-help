This is the repository hosting Supernova pathing's documentation.

To run a local install of the repository first install pnpm globally

```bash
npm i -g pnpm
```

Then install dependencies with

```bash
npm i
```

Then you can run the project locally with 

```bash
pnpm run dev
```
